<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <body>
    <div class="form-group">
      <p><%= f.text_area :url, :class => "form-control", :placeholder => "Place URL to share here" %></p>
      <p></p>
      <p><%= f.text_area :description, :class => "form-control", :rows => "6", :placeholder => "Input description here" %></p>
    </div>

    <div class="form-actions">
      <%= f.submit :class => "btn btn-primary btn-lg" %>
    </div>
    
<% end %>
</body>
